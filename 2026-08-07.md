# 2026-08-07 学习总结

> Stage 0 / W1。今天收获最大的一天：2 道 LC + C++ 概念落地 + Python 两个经典坑 + 桌面整理。

## 今天做了什么

1. **LC#1512 好数对**（暴力 O(n²) + 哈希 O(n)）：`ans += m[num]; ++m[num]` **顺序不能反**（第 k+1 次出现 → 新增 k 对）
2. **C++11：sort + 自定义 cmp**（降序跑通）：`sort(a, a+5, cmp)`，cmp 返回 `a>b`
3. **C++ Primer Ch4 表达式**（没看书，AI 讲 C 老手增量版）：
   - **左值/右值**：`&` 只能对左值；判断技巧"能不能取 &"
   - 赋值返回**左值引用**：`(a=b)=c` 合法
   - 关系/逻辑运算返回 `bool`
   - 显式类型转换：`reinterpret_cast`（寄存器地址映射）/ `static_cast` / `const_cast`
   - **优先级**：`&` 比 `==` 低、`<<` 比 `+` 低（`1<<2+3` = 32 不是 7）
   - **位运算四件套**：置位 `REG|=1<<n` / 清位 `REG&=~(1<<n)` / 翻转 `REG^=1<<n` / 读位 `(REG>>n)&1`
4. **LC#5 合并两个有序链表** ✅（链表初学，手敲 C++ 版，dummy 虚拟头）
5. **Python**：dict 3.7+ **保插入顺序**；列表推导 + 元组解包；切片 `row[:]`（拷贝）+ 迭代（range/enumerate）；杨辉三角（可变对象别名/拷贝、原地修改坑）
6. 桌面整理：删 5 个无用 md 文档；learning-notes push 4 篇 LC 记录

## 卡点 & 踩坑（今天最有价值）

1. **链表"数组思维"**：往空指针写值 = UB。链表要"接线"（`tail->next=节点`），不是"填值"（`list3->val=`）。节点要现造（new）或借用现成的。
2. **悬垂指针**：返回局部变量地址。和 08-04 记的"返回局部数组"同一概念，C/C++ 通用。
3. **dummy 虚拟头**：解决结果链表第一个节点的特例。
4. **Python 可变对象别名**：`b = row` 不复制（同一对象），`row[:]` 才是拷贝。生成器 yield 可变对象要交拷贝。
5. **原地修改坑**：从左往右改会污染还没用的旧值；从右往左改或生成新列表。
6. **桌宠覆盖坑**：桌宠只启动时读一次 data.json，退出/勾选会把旧数据写回覆盖外部修改 → 改清单先关桌宠。
7. **编译/运行坑**：缺头文件（`<algorithm>`/`<cstdint>`）、`/.test` 少点、`list1->next;` 少 `=`（statement has no effect）。

## 明天（08/08）第一件事

- 按执行计划 08/08 行：**C++ Ch5（语句）继续；复盘 LC#1-5**
- Python 切片/迭代已通过今天实践覆盖（够用即可），不单列
