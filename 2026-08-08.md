# 2026-08-08 学习总结

> Stage 0 / W1 收尾。计划外：打比赛坐 6 小时高铁，晚上时间给了 Linux 入门（韦东山）。原计划 C++ Ch5 + 复盘 LC#1-5 顺延。当晚用提问消化法把 GCC/库/Makefile 三个模糊点补透了。

## 今天做了什么

1. **韦东山 Linux 教程**（跳过第 3 章）
   - **交叉编译**概念：PC（x86）上编译 → 开发板（ARM）上运行，编译器架构与目标机不同
   - **GCC 编译四阶段**：预处理（`-E`，展开宏/头文件）→ 编译（`-S`，C→汇编）→ 汇编（`-c`，汇编→目标文件 .o）→ 链接（.o+库→可执行）
   - gcc 命令：`gcc -o 可执行 源`、`-c` 只编译不链接、`ar` 打包库
   - **动态库 .so / 静态库 .a**（当晚消化，见下）
   - gcc 常用选项：`-I` / `-L` / `-l`（当晚理清分工）
2. **Makefile**
   - 依赖关系 + **时间戳对比**：依赖比目标新 → 重编；也检查依赖是否存在
   - 自动变量：`$@` 目标 / `$<` 第一个依赖 / `$^` 全部依赖
   - 函数：`filter` / `filter-out`（过滤/反过滤）、`wildcard`（通配符展开）、`foreach`（遍历）、`patsubst`（模式替换）

## 当晚消化补透的 3 个关键点（最有价值）

1. **动态库的"记账"模型**：静态库链接时**把代码搬进可执行文件**（搬进家），删 .a 也能跑；动态库链接时**只记一条"要用 libfoo.so 里的 foo()"**，代码在**运行时**才由动态链接器 `ld.so` 加载（记地址，用到再上门）。省内存（一份 .so 共享）/省磁盘/换库不重编；代价：部署要带 .so，运行时 ld.so 要能找到（`LD_LIBRARY_PATH`）。
2. **`-I` / `-L` / `-l` 分工**：`-I`（大写）找头文件 `.h`（编译前期）；`-L` 指"去哪个目录找库"；`-l` 点名"要哪个库"（`-lmylib` → `libmylib.so`）。三者都是"加搜索路径/名字"，只是目标不同。
3. **Make 只认依赖列表，不认 include**：`main.o: main.c` 里没写 util.h，改了 util.h 它毫无知觉 → 用旧 .o 链接出诡异 bug。解决：依赖里补头文件，真实工程用 **`gcc -MM`**（扫描 #include 自动吐出 `main.o: main.c util.h` 规则，递归；`-M` 连系统头文件也列，`-MM` 只列工程内头文件）。

## 卡点 & 踩坑

1. 动态库/静态库最初只背了定义，没懂"链接时 vs 运行时"（已消化）
2. `-i` 记混：gcc 没有裸 `-i`，是 `-I`（大写）头文件路径；`-l` 是小写 L 不是数字 1
3. 自动变量漏 `$`：`$<` 记成 `<`
4. 计划内任务没做（时间给了 Linux），顺延不硬追

## 明天（08-09/10 周复盘）注意

- **补欠账**：C++ Primer Ch5（语句）继续 + 复盘 LC#1-5（1/2/9/70/1512 手敲）
- 动手落地：gcc 四阶段 + 动/静态库各编一遍，当场验证今天消化结论
- Makefile 的 `patsubst` / `wildcard` / `foreach` 各写一行验证记忆
