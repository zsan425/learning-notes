# 2026-08-04 学习总结

> 执行计划第一天（Stage 0 / W1）。方向：端侧 AI 部署（嵌入式 AI 方向定稿后首日执行）。

## 今天做了什么

1. **装好 WSL2 + Ubuntu 26.04**（计划写 22.04，实际 26.04，保留）
   - 清华 apt 源（11.6 MB/s），原源已备份
   - 建普通用户 `zsan425` + sudo 组，已改正式密码
   - WSL 内 git 配置完成（zsan425 / noreply 邮箱）
2. **learning-notes 仓库首次 push 到 GitHub** ✅
3. **廖雪峰 Python 前 3 节**：环境 / 数据类型和变量 / 字符串和编码
   - `%` 格式化：成绩提升练习 `print('%.1f%%' % r)` → `18.1%`
4. **LC#1 两数之和**（一题两解，已提交通过）
   - C 暴力版 O(n²)
   - Python dict 版 O(n)
5. **LC#2 有效的括号**（栈，提前完成 ✅）
   - 辅助函数 `pairs()` 统一处理左右括号
   - 关键点：栈顶是 `stack[top-1]`，栈空先判 `top==0`

## 卡点 & 踩坑

1. **`wsl --install` 网络超时**（GitHub 不通）→ 手动安装完成
2. **中文输入法打代码**：全角冒号 `：` + `for` 漏 `in`，反复出现 3 次 —— 写代码前切英文输入法
3. **C 返回局部数组** = 悬垂指针，必须 malloc
4. **`{1, 2, 3}` 是 set 不是 dict**：花括号里带冒号 `:` 才是 dict
5. **dict 的 key 必须不可变**：`(1, 2, 3)` 可以，`(1, [2, 3])`（含 list）报 `TypeError`
6. **Python 交互窗口 `>>>` 里不能贴 shell 命令** —— 先 `exit()` 回终端再运行
7. **环境版本偏差**：系统是 Ubuntu 26.04（resolute），执行计划文档写的 22.04，后续引用命令时注意
8. **LC#2 pairs 调用方向反了**：写 `pairs(stack[top])`（输入左括号返回 0）→ 应 `pairs(s[i])`（输入当前字符）
9. **栈顶索引**：top 是元素个数，栈顶在 `stack[top-1]` 不是 `stack[top]`；不匹配要 `return false`，不是继续压栈

## 明天（08/05）

- 廖雪峰：**列表/元组/条件/循环快扫**（list/for/dict 今天已用过，只补 tuple/while/break/continue）
- C++ Primer **Ch2**（类型/字符串，跳过 Ch1——C 老手）
- ~~LC#2 有效的括号~~（已提前完成）
- 顺手：把 LC#1 dict 版和 LC#2 再手敲一遍（手敲红线）
